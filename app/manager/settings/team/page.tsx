export default function TeamPage() {
  return (
    <div className="mgr-card">
      <h2>Team &amp; permissions</h2>
      <p className="card-sub">Staff accounts, roles and permissions for this portal.</p>
      <p className="mgr-empty">
        Not built yet — the manager portal currently has no login at all, which also means anyone with the URL can use
        it. Building the staff login (and this Team panel with it) is the top follow-up task.
      </p>
    </div>
  );
}
