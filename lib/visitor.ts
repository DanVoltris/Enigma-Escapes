// The visitor cookie's name, on its own so the client component that sets it
// can import it without dragging the server-side events module (and Supabase)
// into the browser bundle.
export const VISITOR_COOKIE = "vb_vid";
